<h1>Editing job_application</h1>

<% form_for(@job_application, :html => { :multipart => true }) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => f %>

  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @job_application %> |
<%= link_to 'Back', job_applications_path %>